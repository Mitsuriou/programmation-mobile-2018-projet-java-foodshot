<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ca.qc.cgmatane.informatique.foodshot">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".ActivitePrincipale"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ActiviteNouvellePublication"
            android:label="@string/titre_activite_nouvelle_publication" />
        <activity
            android:name=".ActiviteMonProfil"
            android:label="@string/titre_activite_profil"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ActiviteRechercherProfil"
            android:label="@string/titre_activite_rechercher_profil" />
        <activity
            android:name=".ActivitePublicationsAimees"
            android:label="@string/titre_activite_publications_aimees" />
        <activity
            android:name=".ActivitePersonnesSuivies"
            android:label="@string/titre_activite_personnes_suivies" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".ActiviteCarte"
            android:label="@string/title_activity_activite_carte"></activity>
    </application>

</manifest>